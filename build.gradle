plugins {
    id 'fabric-loom' version '1.10-SNAPSHOT' apply false
    id 'net.neoforged.moddev' version '2.0.78' apply false
}

allprojects {
    rootProject.version = rootProject.version + "+" + rootProject.minecraft_version
}